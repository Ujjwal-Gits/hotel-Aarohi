# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (PostgreSQL)
# Replace user, password, host, and dbname with your local PG credentials
DATABASE_URL="postgresql://user:password@localhost:5432/aarohi?schema=public"

# Authentication (to be implemented)
JWT_SECRET="aarohi_super_secret_key"
JWT_EXPIRES_IN="7d"
